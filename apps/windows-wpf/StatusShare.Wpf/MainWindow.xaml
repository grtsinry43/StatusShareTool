<ui:FluentWindow
    x:Class="StatusShare.WindowsApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:conv="clr-namespace:StatusShare.WindowsApp.Converters"
    mc:Ignorable="d"
    Width="960"
    Height="720"
    Title="StatusShare Windows">
    <Window.Resources>
        <conv:StringToImageSourceConverter x:Key="StringToImageSourceConverter" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Margin="0" Title="StatusShare" />

        <Grid Grid.Row="1" Margin="14,8,14,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Margin="2,0,2,10">
                <TextBlock FontSize="24" FontWeight="SemiBold" Text="StatusShare Windows 客户端" />
                <TextBlock Margin="0,4,0,0" Foreground="#7A7A7A" TextWrapping="Wrap" Text="自动读取当前活动窗口和媒体信息，交给 core 做规则匹配，并在状态变化时立即上报，稳定状态按心跳间隔续报。" />
            </StackPanel>

            <TabControl Grid.Row="1" Margin="0">
                <TabItem Header="首页">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,8,0,8">
                            <ui:Card Margin="0,0,0,16">
                                <Grid Margin="18">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="正在使用 " />
                                            <TextBlock FontSize="22" FontWeight="Bold" Text="{Binding ResolvedName}" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <TextBlock Margin="0,10,0,0" FontSize="15" Text="{Binding ResolvedExtend}" TextWrapping="Wrap" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                                        <Button Margin="0,0,10,0" Padding="16,8" Command="{Binding StartMonitorCommand}" Content="开始监控" />
                                        <Button Padding="16,8" Command="{Binding StopMonitorCommand}" Content="停止监控" />
                                    </StackPanel>

                                    <UniformGrid Grid.Row="2" Grid.ColumnSpan="2" Margin="0,18,0,0" Columns="4">
                                        <ui:Card Margin="0,0,10,0"><StackPanel Margin="12"><TextBlock FontWeight="Bold" Text="监控状态" /><TextBlock Margin="0,6,0,0" Text="{Binding MonitorStatus}" /></StackPanel></ui:Card>
                                        <ui:Card Margin="0,0,10,0"><StackPanel Margin="12"><TextBlock FontWeight="Bold" Text="窗口来源" /><TextBlock Margin="0,6,0,0" Text="{Binding Backend}" /></StackPanel></ui:Card>
                                        <ui:Card Margin="0,0,10,0"><StackPanel Margin="12"><TextBlock FontWeight="Bold" Text="上报原因" /><TextBlock Margin="0,6,0,0" Text="{Binding PushReason}" /></StackPanel></ui:Card>
                                        <ui:Card><StackPanel Margin="12"><TextBlock FontWeight="Bold" Text="心跳间隔" /><TextBlock Margin="0,6,0,0" Text="{Binding IntervalDisplay}" /></StackPanel></ui:Card>
                                    </UniformGrid>

                                    <ui:Card Grid.Row="3" Grid.ColumnSpan="2" Margin="0,18,0,0">
                                        <Grid Margin="16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="88" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Width="72" Height="72" Background="#12000000" CornerRadius="14">
                                                <Image Stretch="UniformToFill" Source="{Binding MediaThumbnailPath, Converter={StaticResource StringToImageSourceConverter}}" />
                                            </Border>
                                            <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                                <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding MediaTitle}" TextWrapping="Wrap" />
                                                <TextBlock Margin="0,6,0,0" Foreground="#7A7A7A" Text="{Binding MediaArtist}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Grid>
                                    </ui:Card>
                                </Grid>
                            </ui:Card>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="配置">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,8,0,8">
                            <ui:Card Margin="0,0,0,16">
                                <Grid Margin="18">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Margin="0,0,12,12" VerticalAlignment="Center" Text="配置文件路径" />
                                    <TextBox Grid.Column="1" Margin="0,0,0,12" Text="{Binding ConfigPath, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="1" Margin="0,0,12,12" VerticalAlignment="Center" Text="Base URL" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Text="{Binding BaseUrl, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="2" Margin="0,0,12,12" VerticalAlignment="Center" Text="Token" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="3" Margin="0,0,12,12" VerticalAlignment="Center" Text="上报间隔（秒）" />
                                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,12" Text="{Binding HeartbeatIntervalSecs, UpdateSourceTrigger=PropertyChanged}" />
                                    <WrapPanel Grid.Row="4" Grid.Column="1">
                                        <Button Margin="0,0,10,0" Padding="16,8" Command="{Binding LoadConfigCommand}" Content="加载配置" />
                                        <Button Margin="0,0,10,0" Padding="16,8" Command="{Binding SaveConfigCommand}" Content="保存配置" />
                                        <Button Padding="16,8" Command="{Binding FetchServerStatusCommand}" Content="获取当前服务端状态" />
                                    </WrapPanel>
                                </Grid>
                            </ui:Card>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="规则">
                    <Grid Margin="0,8,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="320" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ui:Card>
                            <DockPanel Margin="16">
                                <StackPanel DockPanel.Dock="Top">
                                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="规则列表" />
                                    <TextBlock Margin="0,6,0,12" Foreground="#7A7A7A" TextWrapping="Wrap" Text="一条规则代表一种窗口匹配策略。Allow 表示命中后上报，Deny 表示命中后跳过本次上报。" />
                                    <WrapPanel Margin="0,0,0,12">
                                        <Button Margin="0,0,10,0" Padding="16,8" Command="{Binding AddRuleCommand}" Content="新增规则" />
                                        <Button Padding="16,8" Command="{Binding DeleteRuleCommand}" Content="删除规则" />
                                    </WrapPanel>
                                </StackPanel>
                                <ListBox ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="10" Margin="0,0,0,8" Background="#08000000" CornerRadius="10">
                                                <StackPanel>
                                                    <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" />
                                                    <TextBlock Margin="0,4,0,0" Text="{Binding Pattern}" />
                                                    <TextBlock Margin="0,4,0,0" Foreground="#7A7A7A" Text="{Binding Summary}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </DockPanel>
                        </ui:Card>

                        <ui:Card Grid.Column="2">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <Grid Margin="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Margin="0,0,12,12" VerticalAlignment="Center" Text="默认策略" />
                                    <CheckBox Grid.Column="1" Margin="0,0,0,12" IsChecked="{Binding DefaultReport}" Content="未命中规则时默认上报" />
                                    <TextBlock Grid.Row="1" Margin="0,0,12,12" VerticalAlignment="Center" Text="默认名称" />
                                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Text="{Binding DefaultDisplayName, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="2" Margin="0,0,12,12" VerticalAlignment="Center" Text="默认文案" />
                                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Text="{Binding DefaultExtend, UpdateSourceTrigger=PropertyChanged}" />
                                    <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,8,0,16" />
                                    <TextBlock Grid.Row="4" Margin="0,0,12,12" VerticalAlignment="Center" Text="规则 ID" />
                                    <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,12" Text="{Binding SelectedRule.Id, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="5" Margin="0,0,12,12" VerticalAlignment="Center" Text="是否启用" />
                                    <CheckBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,12" IsChecked="{Binding SelectedRule.Enabled}" Content="启用这条规则" />
                                    <TextBlock Grid.Row="6" Margin="0,0,12,12" VerticalAlignment="Center" Text="匹配字段" />
                                    <ComboBox Grid.Row="6" Grid.Column="1" Margin="0,0,0,12" ItemsSource="{Binding MatchFields}" SelectedItem="{Binding SelectedRule.Field}" />
                                    <TextBlock Grid.Row="7" Margin="0,0,12,12" VerticalAlignment="Center" Text="匹配方式" />
                                    <ComboBox Grid.Row="7" Grid.Column="1" Margin="0,0,0,12" ItemsSource="{Binding MatchKinds}" SelectedItem="{Binding SelectedRule.Kind}" />
                                    <TextBlock Grid.Row="8" Margin="0,0,12,12" VerticalAlignment="Center" Text="匹配内容" />
                                    <TextBox Grid.Row="8" Grid.Column="1" Margin="0,0,0,12" Text="{Binding SelectedRule.Pattern, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="9" Margin="0,0,12,12" VerticalAlignment="Center" Text="上报策略" />
                                    <ComboBox Grid.Row="9" Grid.Column="1" Margin="0,0,0,12" ItemsSource="{Binding ReportPolicies}" SelectedItem="{Binding SelectedRule.ReportPolicy}" />
                                    <TextBlock Grid.Row="10" Margin="0,0,12,12" VerticalAlignment="Center" Text="区分大小写" />
                                    <CheckBox Grid.Row="10" Grid.Column="1" Margin="0,0,0,12" IsChecked="{Binding SelectedRule.CaseSensitive}" Content="按大小写精确匹配" />
                                    <TextBlock Grid.Row="11" Margin="0,0,12,12" VerticalAlignment="Center" Text="显示名称" />
                                    <TextBox Grid.Row="11" Grid.Column="1" Margin="0,0,0,12" Text="{Binding SelectedRule.DisplayName, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Grid.Row="12" Margin="0,0,12,0" VerticalAlignment="Center" Text="扩展文案" />
                                    <TextBox Grid.Row="12" Grid.Column="1" AcceptsReturn="True" MinHeight="90" TextWrapping="Wrap" Text="{Binding SelectedRule.Extend, UpdateSourceTrigger=PropertyChanged}" />
                                </Grid>
                            </ScrollViewer>
                        </ui:Card>
                    </Grid>
                </TabItem>

                <TabItem Header="调试">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="0,8,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <ui:Card>
                                <StackPanel Margin="16">
                                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="窗口元数据" />
                                    <TextBlock Margin="0,6,0,16" Foreground="#7A7A7A" TextWrapping="Wrap" Text="这里显示当前 Windows 侧抓到的窗口元数据，便于排查规则为什么命中或没有命中。" />
                                    <TextBlock FontWeight="Bold" Text="窗口来源" /><TextBlock Margin="0,4,0,10" Text="{Binding Backend}" />
                                    <TextBlock FontWeight="Bold" Text="窗口标题" /><TextBlock Margin="0,4,0,10" Text="{Binding WindowTitle}" TextWrapping="Wrap" />
                                    <TextBlock FontWeight="Bold" Text="应用名称" /><TextBlock Margin="0,4,0,10" Text="{Binding AppName}" />
                                    <TextBlock FontWeight="Bold" Text="进程名称" /><TextBlock Margin="0,4,0,10" Text="{Binding ProcessName}" />
                                    <TextBlock FontWeight="Bold" Text="可执行文件路径" /><TextBlock Margin="0,4,0,10" Text="{Binding ExecutablePath}" TextWrapping="Wrap" />
                                    <TextBlock FontWeight="Bold" Text="Bundle ID" /><TextBlock Margin="0,4,0,0" Text="{Binding BundleId}" TextWrapping="Wrap" />
                                </StackPanel>
                            </ui:Card>

                            <ui:Card Grid.Column="2">
                                <StackPanel Margin="16">
                                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="解析结果" />
                                    <TextBlock Margin="0,6,0,16" Foreground="#7A7A7A" TextWrapping="Wrap" Text="这里显示 core 解析出来的结果，以及当前这一轮是否应该发请求。" />
                                    <TextBlock FontWeight="Bold" Text="命中规则" /><TextBlock Margin="0,4,0,10" Text="{Binding MatchedRuleId}" />
                                    <TextBlock FontWeight="Bold" Text="解析名称" /><TextBlock Margin="0,4,0,10" Text="{Binding ResolvedName}" />
                                    <TextBlock FontWeight="Bold" Text="解析文案" /><TextBlock Margin="0,4,0,10" Text="{Binding ResolvedExtend}" TextWrapping="Wrap" />
                                    <TextBlock FontWeight="Bold" Text="上报原因" /><TextBlock Margin="0,4,0,10" Text="{Binding PushReason}" />
                                    <TextBlock FontWeight="Bold" Text="媒体标题" /><TextBlock Margin="0,4,0,10" Text="{Binding MediaTitle}" />
                                    <TextBlock FontWeight="Bold" Text="媒体作者" /><TextBlock Margin="0,4,0,10" Text="{Binding MediaArtist}" />
                                    <TextBlock FontWeight="Bold" Text="媒体缩略图" /><TextBlock Margin="0,4,0,0" Text="{Binding MediaThumbnail}" TextWrapping="Wrap" />
                                </StackPanel>
                            </ui:Card>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="日志">
                    <ui:Card Margin="0,8,0,8">
                        <TextBox Margin="16" AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" Text="{Binding LogOutput}" />
                    </ui:Card>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</ui:FluentWindow>
