<ui:FluentWindow
    x:Class="StatusShare.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d"
    Width="960"
    Height="760"
    Title="StatusShare WPF">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="StatusShare" Subtitle="Windows shell backed by Rust P/Invoke" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,24,0,0">
                <ui:Card Margin="0,0,0,16">
                    <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Base URL" />
                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Text="{Binding BaseUrl, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Token" />
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Text="{Binding Token, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Heartbeat (s)" />
                        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Text="{Binding HeartbeatIntervalSecs, UpdateSourceTrigger=PropertyChanged}" />

                        <CheckBox Grid.Row="3" Grid.Column="1" Content="在线 / OK = 1" IsChecked="{Binding Ok}" />
                    </Grid>
                </ui:Card>

                <ui:Card Margin="0,0,0,16">
                    <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Process" />
                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Text="{Binding Process, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Extend" />
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Text="{Binding Extend, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Media Title" />
                        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Text="{Binding MediaTitle, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Media Artist" />
                        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,12" Text="{Binding MediaArtist, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="Media Thumbnail" />
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding MediaThumbnail, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </ui:Card>

                <WrapPanel Margin="0,0,0,16">
                    <Button Margin="0,0,12,0" Padding="16,8" Command="{Binding FetchCommand}" Content="Fetch" />
                    <Button Margin="0,0,12,0" Padding="16,8" Command="{Binding PushCommand}" Content="Push Once" />
                    <Button Margin="0,0,12,0" Padding="16,8" Command="{Binding StartHeartbeatCommand}" Content="Start Heartbeat" />
                    <Button Padding="16,8" Command="{Binding StopHeartbeatCommand}" Content="Stop Heartbeat" />
                </WrapPanel>

                <ui:Card>
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock FontWeight="SemiBold" Text="Output" />
                        <TextBox Grid.Row="1"
                                 Margin="0,12,0,0"
                                 AcceptsReturn="True"
                                 IsReadOnly="True"
                                 MinHeight="280"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Text="{Binding Output}" />
                    </Grid>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>

